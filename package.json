{
  "name": "project-tet-api",
  "version": "0.0.1",
  "private": true,
  "description": "Home automation API",
  "keywords": [],
  "author": "",
  "license": "",
  "main": "dist/app.js",
  "dependencies": {
    "express": "^4.12.3",
    "node-bluetooth": "^1.2.6",
    "node-cron": "^2.0.3",
    "swagger-express-mw": "^0.1.0",
    "websocket": "^1.0.32"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/swagger-express-mw": "^0.7.7",
    "@types/websocket": "^1.0.1",
    "copyfiles": "^2.4.0",
    "nodemon": "^2.0.4",
    "should": "^7.1.0",
    "supertest": "^1.0.0",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "build": "tsc && npm run copy:assets",
    "copy:assets": "mkdir -p ./dist/api/swagger && cp ./api/swagger/swagger.yaml ./dist/api/swagger/swagger.yaml",
    "start": "npm run build && node dist/app.js",
    "start:prod": "tsc && nohup node dist/app.js",
    "deploy": "nohup ngrok http -hostname=zeenotheinventor.ngrok.io 10010",
    "test": "swagger project test"
  }
}